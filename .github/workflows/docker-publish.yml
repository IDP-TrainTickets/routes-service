name: Build and publish to Dockerhub

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PAT }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ vars.DOCKERHUB_NAMESPACE }}/${{ github.event.repository.name }}:latest
            ${{ vars.DOCKERHUB_NAMESPACE }}/${{ github.event.repository.name }}:${{ github.sha }}
  deploy-andrei:
    runs-on: ["self-hosted", "andrei"]
    if: github.actor == 'andreistan26'
    needs: docker
    steps:
      - run: curl -X POST http://127.0.0.1:9000/api/webhooks/9647dd0e-51f1-4a4a-a7fc-0478f15d944e
